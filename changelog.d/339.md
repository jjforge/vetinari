section: Improvements
- [user] `run` now says it merges nothing: `--help` and the green banner both state the work is banked on `agent/<id>` and name `campaign <id>` as what integrates it, so a green run no longer reads as a finished issue. The banner stays suppressed under `--json` and for a campaign's own child run (#339).

section: Documentation
- [user] The design run-loop contract and the user guide's onboarding ladder now spell out that a run banks work on its branch while the campaign is what merges and gates it (#339).
